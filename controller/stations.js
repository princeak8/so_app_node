import {ugwuaji, adiabor, aba, afamIv_vGs, afamViGs, ahaoda, ajah, ajaokuta, akangba, aladja, alagbon, alaoji, alaojiGs, asaba, ayade, azuraIppGs} from './stations/index';

const send = (wss) => {
    ugwuaji(wss);
    adiabor(wss);
    aba(wss);
    afamIv_vGs(wss);
    afamViGs(wss);
    ahaoda(wss);
    ajah(wss);
    ajaokuta(wss);
    akangba(wss);
    aladja(wss);
    alagbon(wss);
    alaoji(wss);
    alaojiGs(wss);
    asaba(wss);
    ayade(wss);
    azuraIppGs(wss);
}

export default send;
